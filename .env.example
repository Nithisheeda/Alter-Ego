
# Optional. When set, the app calls the Anthropic API directly from the
# browser for live Future-Self feedback. Without it, the app falls back to
# structured mock diagnostics.
VITE_ANTHROPIC_API_KEY=
